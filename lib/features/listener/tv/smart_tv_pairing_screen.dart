import 'package:flutter/material.dart';

import '../shared/listener_placeholder.dart';

/// Recreates `prototype/screens/smart-tv-pairing.html`.
/// F0 placeholder; real implementation in F4.
class SmartTvPairingScreen extends StatelessWidget {
  const SmartTvPairingScreen({super.key});

  @override
  Widget build(BuildContext context) => const ListenerPlaceholder(
        slug: 'smart-tv-pairing',
        title: 'Smart TV Pairing',
      );
}
